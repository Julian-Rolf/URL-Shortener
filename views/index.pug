html
html
    head
        style(type='text/css').
           body {
                width: 100%;
                height: 100%;
                overflow: hidden;
                font-family: Arial, Helvetica, sans-serif;
            }

            form {
                margin: 0;
                position: absolute;
                top: 25%;
                left: 50%;
                -ms-transform: translate(-50%, -50%);
                transform: translate(-50%, -50%);
                text-align: center;
            }

            form input {
                font-size: 21px;
                padding: 5px 10px;
                border-radius: 10px;
            }

            form button {
                background-color: #555555;
                border: none;
                color: white;
                margin: 0px 15px;
                padding: 5px 24px;
                text-decoration: none;
                font-size: 21px;
                border-radius: 10px;
            }

            form label {
                font-size: 34px;
            }
        title URL-shortener
    body
        form(action = "/", method = "POST")
            div
                label(for="urlInput") URL-Shortener
            br
            div
                input(name = "url" id="urlInput" placeholder="Insert URL")
                button(type = "submit") Submit